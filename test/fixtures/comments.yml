<% num_comments.times do |n| %>
  <%= comment_id(n) %>:
    short_id: <%= "c#{n}" %>
    story: <%= rand_story %>
    user: <%= rand_user %>
    parent_comment: <%= rand > 0.7 ? nil : rand_comment %>
    comment: <%= "comment#{n}" %>
    hat: <%= rand_hat %>
<% end %>